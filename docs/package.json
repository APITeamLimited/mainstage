{
  "name": "@apiteam/docs",
  "version": "0.0.0",
  "description": "APITeam Docs",
  "scripts": {
    "install": "rm -rf ./dist && tsc -p tsconfig.json",
    "build": "rm -rf ./dist && tsc -p tsconfig.json"
  },
  "main": "dist/index.js",
  "peerDependencies": {
    "classnames": "*"
  },
  "dependencies": {
    "@apiteam/types": "*"
  },
  "devDependencies": {
    "@types/node": "^18.0.6",
    "@types/ws": "^8.5.3",
    "copyfiles": "^2.4.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  },
  "private": true
}
