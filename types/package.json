{
  "name": "@apiteam/types",
  "version": "0.0.0",
  "description": "APITeam shared datatypes",
  "scripts": {
    "install": "rm -rf ./dist && tsc -p tsconfig.json",
    "build": "rm -rf ./dist && tsc -p tsconfig.json"
  },
  "main": "dist/index.js",
  "peerDependencies": {
    "classnames": "*",
    "react": "*",
    "typescript": "*"
  },
  "devDependencies": {
    "@types/node": "^18.0.6",
    "@types/ws": "^8.5.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  },
  "private": true,
  "dependencies": {
    "@apiteam/env-regex": "*",
    "is-valid-domain": "^0.1.6",
    "zod": "^3.19.1"
  }
}
